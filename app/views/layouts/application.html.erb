<!DOCTYPE html>
<html>
<head>
  <title>Флэшкарточкер</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="nav">
    <% if current_user %>
      <%= link_to "Редактировать профиль", edit_user_path(current_user) %> |
      <%= link_to "Выйти", logout_path, method: :post %> |
      <%= current_user.email %>
    <% else %>
      <%= link_to "Регистрация", new_user_path %> |
      <%= link_to "Войти", login_path %> |
      <%= link_to "Войти через vk.com", auth_at_provider_path(provider: :vk) %>
    <% end %>
  </div>
  <div>
    <% if flash.any? %>
      <% flash.each do |message_type, message| %>
	<p class="alert alert-<%= message_type %>"><%= message %></p>
      <% end %>
    <% end %>
  </div>
  <section id="container">

    <header class="clearfix">
      <h1>Флэшкарточкер</h1>
      <ul class="navigation-links">
	<% if current_user %>
	  <% if current_user.decks.empty? %>
	    <li><%= link_to "Добавить колоду", new_deck_path %></li>
	  <% else %>
            <li><%= link_to "Добавить колоду", new_deck_path %></li>
            <li><%= link_to "Все карточки пользователя",
		    user_cards_path(current_user) %></li>
	    <li><%= link_to "Все колоды пользователя", decks_path %></li>
	  <% end %>
	<% end %>
	<li><%= link_to "Главная", root_path %></li>
      </ul>
    </header>

    <section>
      <p>
        Первый в мире удобный менеджер флеш-карточек. Именно так.
      </p>
    </section>
    
    <section>
      <%= yield %>
    </section>

  </section>

</body>
</html>
